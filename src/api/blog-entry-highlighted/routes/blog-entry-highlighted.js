'use strict';

/**
 * blog-entry-highlighted router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::blog-entry-highlighted.blog-entry-highlighted');
